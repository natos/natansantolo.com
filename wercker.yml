box: golang:latest
build:
  steps:
    - arjen/hugo-build:
        version: "0.53"
deploy:
  steps:
    - uetchy/gh-pages:
      token: $GIT_TOKEN
      repo: natansantolo.com/natos.github.com
      domain: www.natansantolo.com
      path: public
